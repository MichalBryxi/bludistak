<%= simple_form_for(@worm) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.association :project, include_blank: "Vyber!" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
