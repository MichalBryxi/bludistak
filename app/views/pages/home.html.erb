<div class="bs-example">
    <div id="myCarousel" class="carousel slide" data-interval="3000" data-ride="carousel">
    	<!-- Carousel indicators -->
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
            <li data-target="#myCarousel" data-slide-to="2"></li>
        </ol>   
       <!-- Carousel items -->
        <div class="carousel-inner">
            <div class="active item">
                <h2>Začni hned!</h2>
                <div class="carousel-caption">
                  <h3>Login</h3>
                  <p>Vstup do aplikace je zdarma. Stačí se přihlásit přes <%= link_to "Facebook", user_omniauth_authorize_path(:facebook) %>, nebo <%= link_to "Twitter", user_omniauth_authorize_path(:twitter) %></p>
                </div>
            </div>
            <div class="item">
                <h2>Bludišťákuj!</h2>
                <div class="carousel-caption">
                  <h3>Worms</h3>
                  <p>Naštval tě někdo? Udělal ti radost? Chceš ho prostě patřičně ohodnotit? </p>
                    <% if user_signed_in? %>
                      Prostě mu  <%= link_to 'dej bludišťáka', new_worm_path %>.
                    <% else %>
                      <%= link_to 'Login', '/login' %>
                    <% end %>
                </div>
            </div>
            <div class="item">
                <h2>Rozdej je všechny!</h2>
                <div class="carousel-caption">
                  <h3></h3>
                  <p><%= image_tag "rada_bludistaku.jpg" , class:"img-rounded" %>
                    <%= image_tag "rada_bludistaku.jpg" , class:"img-rounded" %>
                    <%= image_tag "rada_bludistaku.jpg" , class:"img-rounded" %></p>
                </div>
            </div>
        </div>
        <!-- Carousel nav -->
        <a class="carousel-control left" href="#myCarousel" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
        </a>
        <a class="carousel-control right" href="#myCarousel" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
        </a>
    </div>
</div>                             		