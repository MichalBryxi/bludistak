
<div class="navbar-collapse collapse">
				
  <ul class="nav navbar-nav">
          <li><%= current_user.name if current_user %></li>
          <li></li>
          <li></li>
          <li></li>
          <li class="active"><%= link_to 'Home', "/" %></li>
					<li><%= link_to 'New Idea', new_idea_path %></li>
					<li><%= link_to 'Worms', worms_path %></li>
          <li><%= link_to 'Projects' , projects_path %></li>
					<li class="dropdown">
            <% if user_signed_in? %>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= current_user.name %> <b class="caret"></b></a>
            <% else %>
             	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Přihlásit <b class="caret"></b></a>
            <% end %>
						<ul class="dropdown-menu">
              <% if user_signed_in? %>
                <li><%= link_to "Odhlásit", destroy_user_session_path, method: :delete %></li>
              <% else %>       
                <li><%= link_to "Facebook", user_omniauth_authorize_path(:facebook) %></li>
                <li><%= link_to "Twitter", user_omniauth_authorize_path(:twitter) %></li>
                <li class="divider"></li>        
              <% end %>      
						</ul>
					</li>
					<li></li>
				</ul>
			
</div>

